# Access Code Mapping Notes

**Status: REVIEW-ONLY ACCESS V2 MAPPING NOTE.** These mappings describe the current typed access v2 diagnostic categories. They do not choose a final access metric and do not promote any access assignment product.

## Typed V2 Raw Codes

Typed v2 access records preserve the raw `ACCESS_CONTROL` code and a workflow-normalized access category.

Confirmed diagnostic mapping:

| Raw code | Review category | Note |
|---|---|---|
| `U` | `unrestricted_or_full_access` | Existing typed v2 mapping. |
| `RIRO` | `right_in_right_out` | Existing typed v2 mapping. |
| `R` | `right_in_right_out` | Confirmed correction; previously fell into `other_review`. |
| `RC` | `right_in_right_out` | Confirmed correction; previously fell into `other_review`. |
| `RIO` | `right_in_only` | Existing typed v2 mapping. |
| `ROO` | `right_out_only` | Existing typed v2 mapping. |
| `LIRIRO` | `restricted_partial_access` | Existing typed v2 mapping. |
| blank | `unknown` | Missing source code. |
| `I` | `other_review` | Kept as review bucket. |
| `M` | `other_review` | Kept as review bucket. |
| `S` | `other_review` | Kept as review bucket. |
| `AS` | `other_review` | Kept as review bucket. |
| `AU` | `other_review` | Kept as review bucket. |

`other_review` is a workflow review bucket, not a native final source-layer category. Raw codes should remain preserved in outputs so later review can refine category meaning without losing source evidence.

## Current Review-Only Audit

The corrected mapping is applied read-only in:

`work/output/roadway_graph/review/current/typed_access_rule_overlap_audit/`

Generated by:

```powershell
.\.venv\Scripts\python.exe -m src.active.roadway_graph.typed_access_rule_overlap_audit
```

The audit compares typed v2 access capture across:

- spatial 100 ft assignment,
- conservative Travelway-windowed assignment,
- broad Travelway-normalized assignment.

Spatial 100 ft remains the conservative primary review evidence. Conservative Travelway-windowed access is high-confidence supplemental evidence. Broad Travelway-normalized access is sensitivity/source-coverage evidence unless broad-only records pass stricter signal-window or manual review.

The final review-only access doctrine and baseline freeze lives under:

`work/output/roadway_graph/review/current/final_access_baseline_freeze/`

Generated by:

```powershell
.\.venv\Scripts\python.exe -m src.active.roadway_graph.final_access_baseline_freeze
```

It carries the corrected typed v2 categories into final typed summaries while preserving raw codes and prior categories through the rule-overlap audit lineage.
