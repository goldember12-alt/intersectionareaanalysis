# Current Repo State 2026-04-16

## What is working now

- The standard bounded CLI slice is runnable and its current outputs are present:
  - `stage-inputs`
  - `normalize-stage`
  - `build-study-slice`
  - `enrich-study-signals-nearest-road`
  - `check-parity`
- `work/output/stage1b_study_slice/` currently contains:
  - `Study_Roads_Divided.parquet`
  - `Study_Signals.parquet`
  - `Study_Signals_NearestRoad.parquet`
- `work/parity/` currently contains:
  - `stage1b_study_slice_qc.json`
  - `stage1b_signal_nearest_road_qc.json`
  - `stage1_parity_manifest.json`
- `python -m src.active.directionality_experiment` has been restored and now writes grouped outputs under `work/output/directionality_experiment/`.
- `python -m src.active.upstream_downstream_prototype` has been restored and now writes grouped outputs under `work/output/upstream_downstream_prototype/`.
- `python -m src.active.high_confidence_upstream_downstream_analysis` has been restored and now writes grouped outputs under `work/output/upstream_downstream_prototype/high_confidence_descriptive_analysis/`.

## What is still provisional

- The standard active workflow remains the small package CLI slice exposed through `-m src`.
- `directionality_experiment`, `upstream_downstream_prototype`, and `high_confidence_upstream_downstream_analysis` are restored but still direct-entry only.
- `high_confidence_upstream_downstream_analysis` is best read as a restored downstream extension, not yet as a standard CLI-integrated step.
- The prototype and high-confidence steps depend on regenerated upstream grouped outputs rather than being standalone workflows.

## What is messy but non-blocking

- Some output areas have been hardened to grouped `current/` contracts without fully cleaning legacy flat-layout residue from earlier runs.
- The clearest example is `work/output/upstream_downstream_prototype/high_confidence_descriptive_analysis/`, where old flat root files still coexist with the new grouped layout.
- Provider-managed file behavior in this OneDrive-backed workspace can block delete or replace operations on older output files, so manual cleanup may still be needed in some places.
- Manual QGIS PNG products are not regenerated by the Python workflow. The Python steps only recreate the tabular, JSON, Markdown, and GeoJSON support layers.

## Current trust posture

- Trust `docs/methodology/overview_methodology.md`, `AGENTS.md`, and `docs/workflow/active_workflow.md` for the current repo contract.
- Trust grouped `current/` output lanes and root `README.md` files inside active output areas over older loose files.
- Treat dated recovery and cleanup memos under `docs/legacy/` as historical context, not current workflow authority.

## Next likely work areas

- Decide whether any restored direct-entry modules should be promoted into the package CLI later.
- Clean legacy flat-layout residue from restored output areas where that can be done safely.
- Keep shrinking the active docs surface so the standard CLI slice plus the restored downstream chain are easy to understand in one pass.
